---
const title = "Lisa Dahl's Portfolio"
const author = "Lisa Dahl's Portfolio";
const keywords = "comics";
const featuredImage = "https://lisadahl.co/assets/logo.png";
const url = "https://lisadahl.co/";
const description = "Lisa Dahl's properly professional portfolio."
const twitterCard = "";
const twitterSite = "";

---

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="robots" content="index, follow" />
  {author && <meta name="author" content={author} />}

  <title>{`${title} | Lisa Dahl`}</title>
  <meta name="title" content={`${title} | Lisa Dahl`} />
  <meta name="description" content={description} />
  {keywords && <meta name="keywords" content={keywords} />}
  {url && <link rel="canonical" href={url} />}

  <meta property="og:title" content={`${title} | Lisa Dahl`} />
  <meta property="og:description" content={description} />
  {featuredImage && <meta property="og:image" content={featuredImage} />}
  {url && <meta property="og:url" content={url} />}
  <meta property="og:type" content="website" />

  {twitterCard && <meta name="twitter:card" content={twitterCard} />}
  <meta name="twitter:title" content={`${title} | Lisa Dahl`} />
  <meta name="twitter:description" content={description} />
  {featuredImage && <meta name="twitter:image" content={featuredImage} />}
  {twitterSite && <meta name="twitter:site" content={twitterSite} />}


</head>